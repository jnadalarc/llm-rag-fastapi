apiVersion: v1
kind: Service
metadata:
  name: ${APP_NAME}
spec:
  type: ClusterIP
  ports:
    - name: "${APP_PORT}tcp"
      port: ${APP_PORT}
      protocol: TCP
      targetPort: ${APP_PORT}
  selector:
    app: ${APP_NAME}